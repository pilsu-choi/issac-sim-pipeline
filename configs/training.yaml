experiment_name: isaac-sim-humanoid-egocentric-perception
dataset_yaml: data/yolo/humanoid_egocentric_perception/dataset.yaml
model_name: yolov8n.pt

training:
  epochs: 10
  image_size: 320
  batch_size: 4
  workers: 2
  device: cpu

tracking:
  mlflow_tracking_uri: http://localhost:5000
  registered_model_name: humanoid-egocentric-object-detector

artifacts:
  output_dir: runs/detect
  run_name: train
